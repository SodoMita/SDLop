# Build output (build/ plus the sanitizer and debug trees: build-asan/, build-dbg/)
build*/

# Editor and OS noise
*.swp
*~
.DS_Store

# Test rig logs (the Wayland rigs write these under /tmp, but a redirected run
# can drop them here)
*.log
